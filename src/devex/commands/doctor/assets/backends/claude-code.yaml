hints:
  doctor_clean: "Setup is healthy — proceed with your task (e.g. `{{ prog }} overview --agent claude-code`)."
  doctor_failures: "Fix the {{ fail_count }} failing check(s) above, then rerun `{{ prog }} doctor --agent claude-code` to confirm."
