hints:
  doctor_clean: "Setup is healthy — proceed with your task (e.g. `{{ prog }} overview --agent codex`)."
  doctor_failures: "Fix the {{ fail_count }} failing check(s) above, then rerun `{{ prog }} doctor --agent codex` to confirm."
