sections:
  - project_memory
  - skills
  - hooks
  - mcp
  - settings
probe: claude_code
hints:
  overview_done: "Pick a skill from the table and run `{{ prog }} explain <skill>`, or `{{ prog }} learn <topic> --agent claude-code` to go deeper, then start your task."
